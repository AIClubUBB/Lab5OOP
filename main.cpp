#include "UI.h"

int main() {
    UI ui=UI();
    ui.run();
    /*string baseUrl;
    baseUrl="https://www.google.com/";
    string completeUrl=baseUrl;
    system(std::string("start " + completeUrl).c_str());*/
	return 0;
}
